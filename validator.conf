{
	instances = {
		validate = PostFieldReader(maperrors = true, 
			field_voornaam = "^\\S.{0,250}$", 
			field_tussenvoegsel = "^.{0,40}$", 
			field_achternaam = "^\\S.{0,250}$",
			field_post = "^\\S+@\\S+$",
			field_email = "^poep$"
		) {
			voornaam_failure_branch = Template(templatefile = f"templates/voornaamfout.html");
			tussenvoegsel_failure_branch = Template(templatefile = f"templates/tussenfout.html");
			achternaam_failure_branch = Template(templatefile = f"templates/achternaamfout.html");
			post_failure_branch = Template(templatefile = f"templates/mailfout.html");
			email_failure_branch = Template(templatefile = f"templates/spam.html");
			form_branch = Return(branchname = "uncool");
			successful_branch = Return(branchname = "cool");
		};

		buddydata = GetFieldReader(maperrors = true,
			field_id = "^[0-9]{1,10}$",
			field_secret = "^[0-9|a-z]{1,250}$"
		) {
			id_failure_branch = Template(templatefile = f"templates/buddyfout.html");
			secret_failure_branch = Template(templatefile = f"templates/buddyfout.html");
			form_branch = Return(branchname = "uncool");
			successful_branch = Return(branchname = "cool");
		}
	};
}