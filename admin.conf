{
	registrations = <f"person/view.conf@overview">;

	teams = <f"team/main.conf@list">;

	instances = {
		begin = Template(f"templates/adminframe.html") {
			content->Dir() {
				main->registrations;
				overview->registrations;
				teams->teams;
			};
		};
	};	
}