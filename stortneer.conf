{
	instances = {
		login = CredentialsStore(
			host = "localhost",	db = "stortneer", credset = "stortneer",
			user = "stortneer", pass = "stortneer", pool = true);
		
		begin = Sessionizer(cookiename = "thegame", checkknown = true) {
			http_branch = SiteSubsection() {
				default_branch = Template(templatefile = f"templates/skelet.html") {
					style_branch = Template(templatefile = f"styles/swag.css");
					content_branch = SiteSubsection() {
						main_branch = Template(templatefile = f"templates/home.html") {
							placecount_branch = MySqlQuerier(credset = "stortneer",
								query = f"counter.sql") {
								single_branch = Template(templatefile = f"templates/counter.html");
							};
						};
						signup_branch = Module(file = f"registration.conf", branch = "begin");
						poke_branch = Module(file = f"registration.conf", branch = "poke");
					};
				};

				static_branch = Module(file = f"static.conf", branch = "begin");

				score_branch = Module(file = f"score/view.conf", branch = "begin");

				aanpassen_branch = Module(file = f"score/controller.conf", branch = "begin");

				problems_branch = Module(file = f"problems/view.conf", branch = "begin");
			};
		};
	};
}