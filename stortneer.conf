{
	instances = {
		login = Connector("stortneer");
		
		begin = Sessionizer(cookiename = "thegame", checkknown = true) {
			http->Dir() {
				default->Template(f"templates/skelet.html") {
					style->Template(f"styles/swag.css");
					content->Dir() {
						main->Template(f"templates/home.html") {
							placecount->DataReader(f"counter.sql") {
								single->Template(f"templates/counter.html");
							};
						};
						poke-><f"registration.conf@poke">;
					};
				};

				static->Fileserver(f"static");

				score-><f"score/view.conf@begin">;

				aanpassen-><f"score/controller.conf@begin">;

				problems-><f"problems/view.conf@begin">;
			};
		};
	};
}
