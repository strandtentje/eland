{
	utf8 = "text/plain; charset=utf-8";

	instances = {
		list = Template(f"templates/main.html") {
			problemindex->FindFile(f"problems") {
				file->Template(f"templates/problemitem.html");
			};
			answerindex->FindFile(f"answers") {
				file->Template(f"templates/answeritem.html");
			};
		};

		get = Dir() {
			problems->Fileserver(f"problems", dot_txt = utf8, dot_py = utf8, dot_html = utf8);
			answers->Fileserver(f"answers");
		};		
	};
}