{
	instances = {
		begin = Template(templatefile = f"skelet.html") {
			content_branch = SiteSubsection() {
				main_branch = Template(templatefile = f"index.html");
				default_branch = Fileserver(rootpath = f"pages", optionalmimetypes = true, 
					allowedmimetypes = { html = "text/html" });
			};
		};
	};
}