{
	instances = {
		register = DataReader(f"register.sql") {
			none->Template(f"templates/registerfail.html");
			single->Template(f"templates/yay.html") {
				email-><f"bevestigingsmail.conf@supply">(remap = { to = "post" });
			};
		};
		
		begin = Template(f"templates/registerform.html") {
			guts->Template(f"templates/formguts.html")
		};

		poke = <f"validator.conf@validate"> {
			uncool->instances.begin;
			cool->instances.register;
		};
	};
}
