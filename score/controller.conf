{
	instances = {
		begin = BasicSingleLogin(username = "lancie", password = "slaapmuts69", realm = "cenc") {
			failure_branch = Template(templatefile = f"templates/fail.html");
			successful_branch = Template(templatefile = f"templates/skelet.html");
		};
	};
}