{
	instances = {
		fail = Template(templatefile = f"templates/fail.html");

		name = PostFieldReader(
			field_naam = "^[\\w|\\s]{1,45}$") {
			form_branch = instances.fail;
			successful_branch = Return(branchname = "writer");
		};

		nameslug = PostFieldReader(
			field_naam = "^[\\w|\\s]{1,45}$",
			field_slug = "^.{1,45}$") {
			form_branch = instances.fail;
			successful_branch = Return(branchname = "writer");
		};

		idname = GetFieldReader(
			field_id = "^[0-9]+$") {
			form_branch = instances.fail;
			successful_branch = instances.name;
		};

		idnameslug = GetFieldReader(
			field_id = "^[0-9]+$") {
			form_branch = instances.fail;
			successful_branch = instances.nameslug;
		}
	};
}