{
	viewnaam = Template(f"templates/viewnaam.html");
	
	viewslugnaam = Template(f"templates/viewslugnaam.html");

	viewprestatieheader = Template(f"templates/header.html") {
		headerops->Return("headerops") {
			default->viewslugnaam;
		};
	};

	viewbehaaldeprestatie = Template(
		f"templates/scorecell.html") {
		scoreops->Return("scoreops");
	};

	viewteamprestaties = DataReader(f"queries/GetBehaald.sql") {
		iterator->viewbehaaldeprestatie;
	};

	viewteam = Template(f"templates/team.html") {
		teamops->Return("teamops") {
			default->viewnaam;
		};
		behaald->viewteamprestaties;
	};

	viewprestatielijst = DataReader(f"queries/GetPrestaties.sql") {
		iterator->viewprestatieheader;
	};

	viewteamlijst = DataReader(f"queries/GetTeams.sql") {
		iterator->viewteam;;
	};

	instances = {
		begin = Template(f"templates/skelet.html") {
			style->Return("style") {
				default->Template(f"styles/view.css");
			};
			metatags->Return("metatags") {
				default->Template(f"templates/autorefresh.html");
			};
			body->Template(f"templates/view.html") {
				prestaties->viewprestatielijst;
				teams->viewteamlijst;
				matrixops->Return("matrixops");
			};
		};
	};
}