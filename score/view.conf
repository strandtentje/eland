{
	instances = {
		begin = Template(templatefile = f"templates/skelet.html") {
			body_branch = Template(templatefile = f"templates/view.html") {
				prestaties_branch = MySqlQuerier(query = f"queries/GetPrestaties.sql", credset = "stortneer") {
					single_branch = Template(templatefile = f"templates/header.html");
					iterator_branch = .single_branch;
				};
				teams_branch = MySqlQuerier(query = f"queries/GetTeams.sql", credset = "stortneer") {
					single_branch = Template(templatefile = f"templates/team.html") {
						behaald_branch = MySqlQuerier(query = f"queries/GetBehaald.sql", 
							credset = "stortneer", params = ["team"]) {
							single_branch = Template(templatefile = f"templates/scorecell.html");
							iterator_branch = .single_branch;
						};
					};
					iterator_branch = .single_branch;
				};
			};
		};
	};
}